# boards/shields/roBa/roBa_R.conf

# 1) EC11系はDTにalps,ec11ノードを用意できるまで無効化
# CONFIG_EC11 is selected only when DT_HAS_ALPS_EC11_ENABLED=y
# 無効化や記述削除で警告を消す
# CONFIG_EC11 is not set
# CONFIG_EC11_TRIGGER_GLOBAL_THREAD is not set

# 2) 未定義シンボルの削除
# PMW3610_ADJUSTABLE_MOUSESPEED=y  <-- 削除

# 3) 非推奨設定の削除（Zephyr新系では使わない）
# NFCT_PINS_AS_GPIOS=y             <-- 削除
# NRF_STORE_REBOOT_TYPE_GPREGRET=y <-- 削除

# 必要に応じて代替（例）
# NFCピンはデフォルトのNFC機能を維持。GPIOとして使いたい場合はSoC/board側のoverlayで設定
# 再起動タイプはZephyrの現行APIに合わせる（必要ならboardsのdefconfigやprj.confで現行項目へ）
